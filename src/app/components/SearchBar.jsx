const SearchBar = () => {
  return (
    <div>
      <input
        type="text"
        placeholder="Search by Name, City, Zip"
        className="border-2 rounded-full px-20 border-red-200"
      />
    </div>
  );
};

export default SearchBar;
